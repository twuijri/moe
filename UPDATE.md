# 🚀 طريقة تحديث النظام على السيرفر (Server Update Guide)

إذا كان لديك النظام يعمل بالفعل على سيرفر سحابي (Cloud Server) وتريد تحديثه لآخر نسخة، اتبع الخطوات التالية:

## 1. الدخول للسيرفر
افتح التيرمينال وادخل للسيرفر:
```bash
ssh root@your-server-ip
cd /path/to/project
```
*(استبدل المسار بالمكان الذي وضعت فيه المشروع)*

## 2. سحب التحديثات (Pull Code)
قم بسحب آخر التعديلات من GitHub:
```bash
git pull origin main
```
*سيسألك عن اسم المستخدم وكلمة المرور إذا كان المخزن خاصاً.*

## 3. إعادة بناء النظام (Rebuild)
بما أننا قمنا بتغييرات في Docker (حذفنا مكتبات ثقيلة)، يجب إعادة البناء:
```bash
docker compose down
docker compose up --build -d
```
*هذا الأمر سيوقف النظام القديم ويشغل النسخة الجديدة الخفيفة.*

## 4. التأكد من العمل
تأكد أن كل شيء يعمل بأمر:
```bash
docker compose logs -f --tail=50
```
سترى `Server running on port 3000`.

---
💡 **ملاحظة:** البيانات القديمة (قاعدة البيانات) لن تُحذف لأنها محفوظة في مجلد `data` الخارجي.
